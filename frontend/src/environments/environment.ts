export const environment = {
  production: false,
  apiUrl: 'https://YOUR-BACKEND-URL.onrender.com/api',
  // How often (ms) the meeting-detail page polls while processing is in progress.
  pollIntervalMs: 4000,
};
